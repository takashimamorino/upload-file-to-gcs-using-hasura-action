{
  "name": "hasura",
  "version": "0.1.0",
  "scripts": {
    "start": "docker-compose up -d",
    "down": "docker-compose down",
    "console": "hasura console"
  },
  "devDependencies": {
    "hasura-cli": "^1.3.3"
  }
}
